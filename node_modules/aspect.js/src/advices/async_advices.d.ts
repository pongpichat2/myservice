import { AsyncAdvice } from '../core/advice';
import { Metadata } from '../core/metadata';
export declare class AsyncOnThrowAdvice extends AsyncAdvice {
    apply(target: Function, metadata: Metadata): Promise<any>;
}
