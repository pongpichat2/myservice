import { Precondition } from '../core/join_point';
import { MethodSelector, PropertySelector } from './selectors';
export declare class MethodPrecondition implements Precondition {
    private selector;
    constructor(selector: MethodSelector);
    assert({classDefinition, methodName}: {
        classDefinition: any;
        methodName: string;
    }): boolean;
}
export declare class MemberPrecondition implements Precondition {
    private selector;
    constructor(selector: PropertySelector);
    assert({classDefinition, fieldName}: {
        classDefinition: any;
        fieldName: string;
    }): boolean;
}
