import { Metadata } from './metadata';
export declare abstract class Advice {
    context: Object;
    advice: Function;
    constructor(context: Object, advice: Function);
    abstract apply(target: Function, metadata: Metadata): void;
    invoke(target: any, metadata: Metadata): any;
}
export declare abstract class AsyncAdvice {
    context: Object;
    advice: Function;
    constructor(context: Object, advice: Function);
    abstract apply<T>(target: Function, metadata: Metadata): Promise<T>;
    invoke(target: any, metadata: Metadata): Promise<any>;
}
