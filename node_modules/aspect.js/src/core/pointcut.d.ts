import { JoinPoint } from './join_point';
import { Advice } from './advice';
export declare class Pointcut {
    joinPoints: JoinPoint[];
    advice: Advice;
    private _applications;
    apply(fn: Function, advisedMetadata: any): void;
}
